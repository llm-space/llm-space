interface:
  display_name: "Electrobun CDP Debug"
  short_description: "Debug the desktop renderer through CDP"
  default_prompt: "Use $electrobun-cdp-debug to inspect the real LLM Space desktop renderer."

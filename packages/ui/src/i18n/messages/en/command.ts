/**
 * The command palette (`apps/desktop/src/components/command-palette.tsx`).
 * Command labels themselves come from {@link enCommandMeta}; this area holds
 * only the palette's own chrome (search placeholder, empty state).
 */
export const enCommand = {
  palette: {
    searchPlaceholder: "Search commands...",
    empty: "No commands found.",
  },
};
